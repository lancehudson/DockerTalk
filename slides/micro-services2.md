## Micro Services - GlobalOptions, Inc

Each service:

* is in its own container
* can be scaled horizontally
* can each have their own dependencies
* can be in the language that best meets the need
* has its own metrics